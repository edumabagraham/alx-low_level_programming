#include <stdio.h>
#include <stdlib.h>

/**
 * alloc_grid - two dimensional array
 * @width: width of array
 * @height: height of array
 *
 * Return: pointer to the a two dimensional
 * array.
 */

int **alloc_grid(int width, int height)
{
int *arr;
if (width <= 0)
{
return (NULL);
}
if (height <= 0)
{
return (NULL);
}




}